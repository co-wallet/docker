# Dev: Vite dev server with hot reload.
# Source code and dependency volumes are mounted at runtime.
FROM node:22-alpine
WORKDIR /app
CMD ["sh", "-c", "if [ ! -x node_modules/.bin/vite ]; then npm ci --prefer-offline; fi && exec npm run dev -- --host"]
