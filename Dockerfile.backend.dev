# Dev: uses air for hot-reload
# Volume-mount ../backend:/app at runtime, this layer just pre-downloads deps
FROM golang:1.25-alpine
WORKDIR /app
RUN go install github.com/air-verse/air@latest
COPY backend/go.mod backend/go.sum ./
RUN go mod download
CMD ["air", "-c", ".air.toml"]
