# Dev: vite dev server with hot-reload
# Volume-mount ../frontend:/app at runtime, this layer just pre-downloads deps
FROM node:22-alpine
WORKDIR /app
COPY frontend/package*.json ./
RUN npm ci
CMD ["npm", "run", "dev", "--", "--host"]
